version: "3.3"
services:
    pinx-nuxt-kit:
        build:
            context: .
        container_name: pinx-nuxt-kit
        restart: unless-stopped
        ports:
            - 3000:3000
        environment:
            NODE_ENV: production
